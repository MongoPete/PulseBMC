# Copy to frontend/.env.local for local dev (never commit real secrets)
AUTH_SECRET=generate-with-openssl-rand-base64-32

# Customer (Aaron)
DEMO_USER=Ajin@test.com
DEMO_USER_PASSWORD=your-customer-password

# Internal / e2e (Pete) — optional second account
DEMO_USER_2=demopete@test.com
DEMO_USER_PASSWORD_2=your-internal-password

API_URL=http://localhost:8000
BACKEND_API_KEY=match-backend-.env
